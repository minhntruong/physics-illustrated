using System;

namespace PhysicsIllustrated.Library.Physics;

public static class Constants
{
    public const float FPS = 60.0f;                   // Frames per second for the game loop
    public const float SECS_PER_FRAME = 1.0f / FPS;   // Milliseconds per frame based on FPS
    public const int PIXELS_PER_METER = 50;           // Conversion factor from meters to pixels
}